// example of feature pattern component
// it contains:
// - container (Container.js)
// -- contains logic, connected to redux or has internal state, event handlers
// - component (AppliedFilters.js)
// -- contains representational component with JSX and styles
// - entry point (index.js)

export { default } from './Container';
