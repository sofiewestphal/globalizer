// here you import and export default at the same time, shorthand
// use index event if you have a single presentational component
// but this HeaderImage should perhaps move to components folder
// components are logicless JSX presentational components
// that can be reused in multiple places
export { default } from './HeaderImage'